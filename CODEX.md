# CODEX.md — VitaShop

> 🔴 **The real file is `AGENTS.md`, not this one.**

Codex automatically loads a file named **`AGENTS.md`** from the repository root. The name `CODEX.md` is not part of any discovery mechanism — no agent will read this file on its own.

**This file exists so you do not go looking.** All the content is in `AGENTS.md` in the same folder.

---

## Root file map

| File | Who loads it automatically |
|---|---|
| `CLAUDE.md` | Claude Code |
| `AGENTS.md` | Codex |
| `CODEX.md` | **Nobody** — a pointer only |

---

## If you are an agent reading this

Go to `AGENTS.md` at the repository root. It points to the memory system and defines the role, the checklists, and the prohibitions.
