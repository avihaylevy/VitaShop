/**
 * Shared focus-visible treatment — DESIGN_SYSTEM.md §4. The rule itself
 * lives once in index.css (`.focus-ring`); this constant is how every
 * primitive references it, so there is one place to change.
 */
export const FOCUS_RING = 'focus-ring'
